/* tslint:disable variable-name prefer-template */

describe('detherJS', () => {
  beforeAll(() => {
    // todo
  });
  afterAll(() => {
    // todo
  });
  describe('tests...', () => {
    test('test 1', () => {
      expect(true).toEqual(true);
    });
  });
});
